/* ═══ EmptySlot — Empty card placeholder ═══ */

export default function EmptySlot() {
  return (
    <div
      className="w-10 h-14 rounded-md flex items-center justify-center text-lg shrink-0 border-2 border-dashed border-slate-600 text-slate-600"
    >
      ?
    </div>
  );
}
